buildscript {
    repositories {
        google()
        mavenCentral() // 替换 jcenter() 为 mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.1'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral() // 替换 jcenter() 为 mavenCentral()
    }
}
